
name: Build and Push to GHCR

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            # 🌟 将 'auunes' 替换为你的 GitHub 用户名或组织名（例如 'c990china'）
            ghcr.io/${{ github.repository_owner }}/dual-ai-chat:latest
            ghcr.io/${{ github.repository_owner }}/dual-ai-chat:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
